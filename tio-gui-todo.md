# tio-gui 待办与优化建议

> 2026-09-09 复核，代码基线：`2a80404`（0.3.6）。
> 本文件仅保留未完成事项和待评估建议；已完成条目及过期发布状态已清除。
> 历史验收见 [软件验收记录](docs/acceptance.md)、[原生串口版说明](docs/serial-portable.md) 和 [0.3.6 发布记录](docs/releases/v0.3.6.md)。
> 功能实现保持 clean-room，不复制参考项目代码。

## P1 — 待完成验收

- [ ] 真实 Windows 安装及串口驱动/收发验收。
  - 已获用户 Windows 10 虚拟机截图确认：安装后可启动，中文、深色界面与四角显示正常。
  - 继续覆盖含空格路径启动、双标签、日志、卸载及配置保留。
  - 加验新版布局、中文/英文切换与重启记忆、系统/浅色/深色主题和四角（普通/最大化/缩放）。
  - 现有 Wine 冒烟不能替代真实 Windows；按 [硬件验收步骤](docs/serial-portable.md#hardware-acceptance) 记录系统、驱动、设备和结果。
- [ ] macOS 中文输入法组合输入人工验收。
  - 覆盖候选选择、组合取消、连续中文与中英文切换，检查实际发送字节无重复、无漏发。
  - 已有 UTF-8 commit / PTY 自动测试不能替代输入法实测。
- [ ] Windows/macOS 原生串口硬件验收。
  - USB 串口实际拔插、自动重连、非标准波特率、数据位/停止位/校验位、Break、DTR/RTS 与 RTS/CTS。
  - 验证断线前排队命令不重放；记录端口名称变化后的行为。
- [ ] Linux 扩展工具硬件验收。
  - RS-485 电气波形、实体 CAN 总线收发、真实 BLE 外设连接/配对/GATT 互操作。
  - 现有 ioctl 模拟、vcan 和模拟 BlueZ 结果只作为软件验收依据。
- [ ] Linux AppImage 原生 FUSE 启动验收。
  - 0.3.6 已记录 extract-and-run 验收；FUSE 挂载路径尚未验证，需在具备 FUSE 的环境检查启动、串口收发和退出。

## P2 — 工程优化建议（尚未实施）

以下来自当前代码和脚本的静态复核；性能风险尚未在本轮实测，不作为已复现缺陷。

- [ ] 补齐统一自动验收入口，优先防止近期修复回退。
  - `tests/acceptance.sh` 未调用 `tests/search_zoom_acceptance.py`、`tests/shell_edit_acceptance.py`，也未构建/运行原生串口版验收。
  - 接入真实 Ctrl+滚轮、Bash/Readline 编辑重放，以及独立 SERIAL_ONLY 构建和 GUI/PTY 测试；缺少依赖时明确报告未覆盖。
  - 验收目标：一条入口可执行已承诺的软件回归，并区分通过、跳过和失败。
- [ ] 增加提交/PR 构建测试及三端发布自动化。
  - `.github/workflows/release.yml` 是当前唯一工作流，仅在 tag/手动触发时创建 release 草稿，没有构建、测试、产物上传或校验步骤。
  - 先接入 Linux 单元/GUI 回归和原生串口构建矩阵，再接入现有打包脚本、包启动检查及 SHA256SUMS 校验。
  - 验收目标：测试失败阻止产物发布，产物可追溯到同一版本提交；真实硬件验收仍单独记录。
- [ ] 评估原生串口日志写入对界面响应的影响。
  - `src/serial_main.c` 的 `tick()` 在 GTK 主线程执行 `fwrite()`，并在每次轮询末尾执行 `fflush()`；轮询间隔 30ms。
  - 先测慢盘/持续接收时的界面延迟和队列占用；必要时迁移到有界后台写入队列，保留写失败停止、溢出提示和退出排空语义。
- [ ] 建立大日志、多标签和持续搜索的性能基线。
  - `src/console_search.h` 每次查找会重新编译模式、复制当前文本、全量匹配并重新应用着色；持续接收时最多每 250ms 刷新一次。
  - 已有匹配预算与容量限制，但匹配预算不覆盖全部 GTK 文本复制和着色工作。
  - 测量接近显示上限时的 CPU、内存、输入延迟和滚动帧耗时，再评估模式/结果缓存及增量着色；保留搜索选区和即时显示行为。
- [ ] 拆分主程序和复用测试构建依赖。
  - `src/main.c` 仍约 6,000 行；多个 GUI 测试直接包含该文件，`CMakeLists.txt` 多次重复列出大批源文件。
  - 公共布局/样式已抽到 `src/workspace_ui.h`；继续拆出会话生命周期、搜索/显示、发送工具和剩余设置逻辑，并复用公共库或对象库；以现有行为回归为验收标准，分步落地。

## P3 — 产品范围候选（待决定，不视为缺陷）

- [ ] 评估原生串口版按 USB 稳定身份重连：当前只按相同 COM 号或 `/dev/cu.*` 路径重连，名称变化需重新选择。
- [ ] 评估 macOS 更低系统版本/Intel 支持，以及 macOS 公证、Windows 发布者签名；当前包为 Apple Silicon/macOS 26+，macOS 使用 ad-hoc 签名。
- [ ] 明确两版功能对齐的优先级：原生版目前不含发送序列、CRC/延迟快捷发送、协议工具、插件和文件传输等 Linux 功能。
- [ ] 如确有使用需求，再评估完整 VT/宽字符终端语义、MQTT TLS/QoS 2；这些属于扩展范围，不能直接当作当前版本遗漏。

## 维护约定

- 完成后把验证结果写入对应验收文档，再删除本文件中的条目。
- 新问题记录复现条件、影响和验收标准；建议与已确认缺陷分开描述。
- 软件模拟通过不等同真实硬件通过；记录未经验证的边界。
